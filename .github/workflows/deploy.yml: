name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: Run API
    runs-on: Caller-Runner
    steps:
      - name: Build API
        run: |
          echo "Build API"
